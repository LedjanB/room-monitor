{
  "name": "hellocare-room-monitor",
  "version": "1.0.0",
  "description": "Shared real-time room/device status board on Firebase. No build step — plain browser ES modules; the Firebase SDK is loaded from the gstatic CDN, not npm. This file exists only so the firebase-tools CLI (run via npx) has a project root.",
  "private": true
}
